# skrol

Monorepo workspace with shared configuration packages.

## Packages

- `@repo/eslint-config`: shared ESLint base config
- `@repo/typescript-config`: shared TypeScript config presets

## Workspace layout

- `apps/`: reserved for applications
- `packages/`: shared packages and config
